import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  cardContainerStyle: {
    borderWidth: 0,
    flexGrow: 1,
    margin: 0, 
    shadowOpacity: 0 
  },
  title: {
    marginBottom: 10,
    textAlign: 'center',
  },
});

export default styles;